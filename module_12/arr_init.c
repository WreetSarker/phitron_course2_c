#include<stdio.h>

int main(){

    int n = 10;
    char arr[] = {'h', 'e', 'l', 'l', 'o'};

    int sz = sizeof(arr)/ sizeof(arr[0]);

    for(int i = i; i< sz; i++){
        putchar(arr[i]);
    }

    return 0;
}
